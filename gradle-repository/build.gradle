group 'com.sunny'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    /*添加对子项目依赖*
     */
    compile project (':gradle-model')
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
