group 'com.sunny'
version '1.0-SNAPSHOT'

apply plugin: 'war'
repositories {
    mavenCentral()
}

dependencies {
    compile project(':gradle-repository')
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
